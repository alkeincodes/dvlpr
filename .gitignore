/target
.DS_Store
*.log

# libghostty-vt build artifacts (generated by `zig build`, not committed)
/vendor/libghostty-vt/zig-out
/vendor/libghostty-vt/.zig-cache

# Local Claude Code worktrees (per-session, never committed). Skills under
# `.claude/skills/` ARE tracked — they are project-level tooling.
/.claude/worktrees/

# Internal design docs (AI/workflow scratch) — not for the public repo
docs/superpowers/
